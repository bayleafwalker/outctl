[package]
name = "outctl-engine"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
libc = "0.2"
outctl-contracts = { path = "../outctl-contracts" }
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[lib]
path = "src/lib.rs"
